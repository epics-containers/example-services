# Container launcher

This folder adds a container image that will launch the simulation beamline.

The container includes:
- docker CLI and docker-compose
- The repository source code which includes the compose files

